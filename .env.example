# Database
# For SQLite (stored on external drive):
DATABASE_URL=sqlite:/media/carsonminor/CarsonMinor1TB/nostr-relay-storage/relay.db
# For PostgreSQL (if using instead of SQLite):
# DATABASE_URL=postgresql://username:password@localhost:5432/nostr_research

# Lightning Network
LND_HOST=localhost:8080
LND_MACAROON_PATH=/path/to/admin.macaroon
LND_TLS_CERT_PATH=/path/to/tls.cert

# Server
PORT=8080
HOST=0.0.0.0  # Use 0.0.0.0 to allow external access, localhost for local only
RELAY_NAME=Research Journal Relay
RELAY_DESCRIPTION=Academic research papers on Nostr with peer review
RELAY_PUBKEY=your_relay_pubkey_here
RELAY_CONTACT=admin@example.com

# Storage
STORAGE_PATH=/media/carsonminor/CarsonMinor1TB/nostr-relay-storage
# Alternative mount points if different:
# STORAGE_PATH=/mnt/CarsonMinor1TB/nostr-relay-storage
# STORAGE_PATH=/home/carsonminor/CarsonMinor1TB/nostr-relay-storage
MAX_CONTENT_SIZE=52428800
DATABASE_PATH=/media/carsonminor/CarsonMinor1TB/nostr-relay-storage/relay.db

# Pricing (satoshis per MB per year)
BASE_PRICE_PER_MB_YEAR=1000
COMMENT_PRICE_PER_MB=100